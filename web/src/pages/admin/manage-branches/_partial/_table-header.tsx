export default function TableHeader() {
  return (
    <thead className="bg-blue-600 text-sm">
      <tr>
        <th className="!py-2 !text-center !text-white">No.</th>
        <th className="!py-2 !text-center !text-white">Tên Chi nhánh</th>
        <th className="!py-2 !text-center !text-white">Quản lý</th>
        <th className="!py-2 !text-center !text-white">SĐT</th>
        <th className="!py-2 !text-center !text-white">Email</th>
        <th className="!py-2 !text-center !text-white">Địa Chỉ</th>
        <th className="!py-2 !text-center !text-white"></th>
      </tr>
    </thead>
  );
}
